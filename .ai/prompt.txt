EXISTING REPO FILES (read these first to match style and patterns):
src/app.module.ts
src/attachments/attachments.module.ts
src/attachments/attachments.service.ts
src/audit/audit.module.ts
src/audit/audit.service.ts
src/auth/auth.controller.ts
src/auth/auth.guard.ts
src/auth/auth.module.ts
src/auth/auth.service.ts
src/auth/dto/login.dto.ts
src/auth/dto/register.dto.ts
src/auth/jwt.strategy.ts
src/auth/public.decorator.ts
src/auth/roles.decorator.ts
src/auth/roles.guard.ts
src/comments/comments.module.ts
src/comments/comments.service.ts
src/dashboard/dashboard.module.ts
src/dashboard/dashboard.service.ts
src/database/database.controller.ts
src/database/database.module.ts
src/database/database.service.ts
src/database/database.types.ts
src/database/kysely.module.ts
src/database/kysely.provider.ts
src/database/migrate.ts
src/database/migrations/001_init.sql
src/database/migrations/001_init_users.ts
src/database/migrations/001_initial.ts
src/database/migrations/002_create_users.ts
src/database/types.ts
src/main.ts
src/modules/database/database.module.ts
src/modules/database/database.types.ts
src/modules/database/kysely.provider.ts
src/notifications/notifications.module.ts
src/notifications/notifications.service.ts
src/projects/dto/create-project.dto.ts
src/projects/dto/update-project.dto.ts
src/projects/projects.controller.ts
src/projects/projects.module.ts
src/projects/projects.service.ts
src/tasks/dto/create-task.dto.ts
src/tasks/dto/update-task.dto.ts
src/tasks/tasks.controller.ts
src/tasks/tasks.module.ts
src/tasks/tasks.service.ts
src/users/dto/user-response.dto.ts
src/users/users.controller.ts
src/users/users.module.ts
src/users/users.service.ts

DIRECT AGENT INSTRUCTION:

You are Claude Code working inside a real git repository.
Implement exactly ONE backend story.
Modify and create repository files directly.
Do not explain. Do not summarize. Do not output markdown. Only make code changes.

Stack: NestJS + Kysely + PostgreSQL

BEFORE WRITING CODE — read and understand these files:
- src/app.module.ts
- src/database/database.service.ts
- src/database/types.ts
- src/auth/auth.guard.ts
- src/auth/jwt.strategy.ts
- src/auth/public.decorator.ts
- src/auth/roles.decorator.ts

Task:
TASK: Organizations Module

READ THESE FILES FIRST before writing any line of code:
- src/app.module.ts
- src/database/database.service.ts
- src/database/types.ts
- src/auth/auth.service.ts
- src/auth/auth.guard.ts
- src/auth/roles.guard.ts
- src/auth/roles.decorator.ts
- src/auth/public.decorator.ts
- src/auth/jwt.strategy.ts
- src/auth/dto/register.dto.ts

CRITICAL RULES — never break these:
1. Guards are GLOBAL via APP_GUARD — do NOT add @UseGuards() to any controller or method
2. Use @Public() for unauthenticated routes, @Roles('Admin') for role-restricted routes
3. jwt.strategy.ts validate() returns { id, email, role, tenant_id } — always use req.user.id, NEVER req.user.sub
4. Role values are EXACTLY: Admin, HRManager, Member — never ADMIN, never all caps
5. Role is NEVER set from request body — always set server-side, remove role from RegisterDto entirely
6. Use ONLY src/database/database.service.ts for DB access — do NOT create new database providers
7. DatabaseModule is @Global() — do NOT import it in feature modules
8. Every query MUST filter by tenant_id
9. UUID PKs always via uuidv4() — never DB-generated
10. Never return password_hash in any response
11. New migrations start at 003_ — 001 and 002 already exist
12. Every module needs: module file, controller, service, DTOs — never skip controller

MODULE STRUCTURE TO CREATE:
- src/organizations/organizations.module.ts
- src/organizations/organizations.controller.ts
- src/organizations/organizations.service.ts
- src/organizations/dto/create-organization.dto.ts

FILES TO MODIFY:
- src/app.module.ts

DATABASE CHANGES:

Create file: src/database/migrations/003_create_refresh_tokens.ts

Full content:

import { Kysely, sql } from 'kysely';

export async function up(db: Kysely<any>): Promise<void> {
  await db.schema
    .createTable('refresh_tokens')
    .addColumn('id', 'uuid', (col) => col.primaryKey().notNull())
    .addColumn('user_id', 'uuid', (col) => col.notNull())
    .addColumn('token_hash', 'text', (col) => col.notNull())
    .addColumn('expires_at', 'timestamptz', (col) => col.notNull())
    .addColumn('created_at', 'timestamptz', (col) =>
      col.notNull().defaultTo(sql`now()`)
    )
    .addColumn('revoked_at', 'timestamptz')
    .addForeignKeyConstraint(
      'refresh_tokens_user_id_fk',
      ['user_id'],
      'users',
      ['id'],
      (cb) => cb.onDelete('cascade')
    )
    .execute();

  await db.schema
    .createIndex('refresh_tokens_user_id_idx')
    .on('refresh_tokens')
    .column('user_id')
    .execute();

  await db.schema
    .createIndex('refresh_tokens_expires_at_idx')
    .on('refresh_tokens')
    .column('expires_at')
    .execute();

  await db.schema
    .createIndex('refresh_tokens_token_hash_unique')
    .on('refresh_tokens')
    .column('token_hash')
    .unique()
    .execute();

  await db.schema
    .createTable('project_members')
    .addColumn('id', 'uuid', (col) => col.primaryKey().notNull())
    .addColumn('project_id', 'uuid', (col) => col.notNull())
    .addColumn('user_id', 'uuid', (col) => col.notNull())
    .addColumn('created_at', 'timestamptz', (col) =>
      col.notNull().defaultTo(sql`now()`)
    )
    .addForeignKeyConstraint(
      'project_members_project_id_fk',
      ['project_id'],
      'projects',
      ['id'],
      (cb) => cb.onDelete('cascade')
    )
    .addForeignKeyConstraint(
      'project_members_user_id_fk',
      ['user_id'],
      'users',
      ['id'],
      (cb) => cb.onDelete('cascade')
    )
    .execute();

  await db.schema
    .createIndex('project_members_user_id_idx')
    .on('project_members')
    .column('user_id')
    .execute();

  await db.schema
    .createIndex('project_members_unique_idx')
    .on('project_members')
    .columns(['project_id', 'user_id'])
    .unique()
    .execute();
}

export async function down(db: Kysely<any>): Promise<void> {
  await db.schema.dropTable('project_members').execute();
  await db.schema.dropTable('refresh_tokens').execute();
}

ENDPOINTS TO IMPLEMENT:

POST /api/organizations
Decorator: @Public()
Request body:
{
  name: string;
  adminEmail: string;
  password: string;
}
Response shape:
{
  organizationId: string;
  userId: string;
}
Service logic:
  1. Generate organizationId = uuidv4()
  2. Insert into organizations table
  3. Hash password using bcrypt.hash(password, 12)
  4. Generate userId = uuidv4()
  5. Insert user with:
     - id: userId
     - email: adminEmail
     - password_hash: hashed password
     - role: 'Admin'
     - is_active: true
     - organization_id = organizationId (IMPORTANT: this is tenant_id equivalent)
  6. Return { organizationId, userId }

KYSELY PATTERNS TO USE:

Insert organization:
await this.db
  .insertInto('organizations')
  .values({
    id: organizationId,
    name: dto.name
  })
  .execute();

Insert admin user:
await this.db
  .insertInto('users')
  .values({
    id: userId,
    email: dto.adminEmail,
    password_hash: hashedPassword,
    role: 'Admin',
    is_active: true,
    organization_id: organizationId
  })
  .execute();

CONTROLLER IMPLEMENTATION:

File: src/organizations/organizations.controller.ts

import { Controller, Post, Body } from '@nestjs/common';
import { Public } from '../auth/public.decorator';
import { OrganizationsService } from './organizations.service';
import { CreateOrganizationDto } from './dto/create-organization.dto';

@Controller('organizations')
export class OrganizationsController {
  constructor(private readonly organizationsService: OrganizationsService) {}

  @Public()
  @Post()
  create(@Body() dto: CreateOrganizationDto) {
    return this.organizationsService.create(dto);
  }
}

SERVICE IMPLEMENTATION:

File: src/organizations/organizations.service.ts

import { Injectable } from '@nestjs/common';
import { v4 as uuidv4 } from 'uuid';
import * as bcrypt from 'bcrypt';
import { DatabaseService } from '../database/database.service';
import { CreateOrganizationDto } from './dto/create-organization.dto';

@Injectable()
export class OrganizationsService {
  constructor(private readonly db: DatabaseService) {}

  async create(dto: CreateOrganizationDto) {
    const organizationId = uuidv4();
    const userId = uuidv4();

    await this.db
      .insertInto('organizations')
      .values({
        id: organizationId,
        name: dto.name,
      })
      .execute();

    const password_hash = await bcrypt.hash(dto.password, 12);

    await this.db
      .insertInto('users')
      .values({
        id: userId,
        email: dto.adminEmail,
        password_hash,
        role: 'Admin',
        is_active: true,
        organization_id: organizationId,
      })
      .execute();

    return {
      organizationId,
      userId,
    };
  }
}

DTO IMPLEMENTATION:

File: src/organizations/dto/create-organization.dto.ts

import { IsEmail, IsString, MinLength } from 'class-validator';

export class CreateOrganizationDto {
  @IsString()
  name: string;

  @IsEmail()
  adminEmail: string;

  @IsString()
  @MinLength(6)
  password: string;
}

MODULE IMPLEMENTATION:

File: src/organizations/organizations.module.ts

import { Module } from '@nestjs/common';
import { OrganizationsController } from './organizations.controller';
import { OrganizationsService } from './organizations.service';

@Module({
  controllers: [OrganizationsController],
  providers: [OrganizationsService],
})
export class OrganizationsModule {}

APP MODULE UPDATE:

Open src/app.module.ts

- Import OrganizationsModule
- Add it to imports array
- Ensure no duplicate imports

Example:

import { OrganizationsModule } from './organizations/organizations.module';

@Module({
  imports: [
    OrganizationsModule,
  ],
})
export class AppModule {}

VERIFICATION — before finishing, check:
- POST /api/organizations is marked with @Public()
- Password is hashed with bcrypt (12 rounds)
- Role is hardcoded to 'Admin'
- No role field accepted from request body
- UUIDs generated via uuidv4()
- No password_hash returned in response
- OrganizationsModule added to app.module.ts
- Migration file is named correctly (003_...)
- Tables and indexes match specification exactly