=== COMPLETED STORIES ===
Stories already implemented: 1, 2, 3
Do NOT re-implement these. Build on top of existing code.

=== CURRENT REPO FILE TREE ===
.ai/
  prompt.txt
  state.json
.env.example
nest-cli.json
package.json
README.md
src/
  app.module.ts
  attachments/
    attachments.module.ts
    attachments.service.ts
  audit/
    audit.controller.ts
    audit.module.ts
    audit.service.ts
  auth/
    auth.controller.ts
    auth.guard.ts
    auth.module.ts
    auth.service.ts
    dto/
      login.dto.ts
      register.dto.ts
    jwt.strategy.ts
    public.decorator.ts
    roles.decorator.ts
    roles.guard.ts
  comments/
    comments.controller.ts
    comments.module.ts
    comments.service.ts
    dto/
      create-comment.dto.ts
  dashboard/
    dashboard.controller.ts
    dashboard.module.ts
    dashboard.service.ts
  database/
    database.module.ts
    database.service.ts
    migrate.ts
    migrations/
      001_init.sql
      001_initial.ts
      001_init_users.ts
      002_create_users.ts
      003_create_refresh_tokens.ts
      004_fix_tasks_columns.ts
    types.ts
  main.ts
  notifications/
    notifications.controller.ts
    notifications.module.ts
    notifications.service.ts
  organizations/
    dto/
      create-organization.dto.ts
    organizations.controller.ts
    organizations.module.ts
    organizations.service.ts
  projects/
    dto/
      create-project.dto.ts
      update-project.dto.ts
    projects.controller.ts
    projects.module.ts
    projects.service.ts
  tasks/
    dto/
      create-task.dto.ts
      update-task.dto.ts
    tasks.controller.ts
    tasks.module.ts
    tasks.service.ts
  users/
    dto/
      user-response.dto.ts
    users.controller.ts
    users.module.ts
    users.service.ts
test.js
tsconfig.json

=== EXISTING MIGRATIONS ===
  - 001_init.sql
  - 001_initial.ts
  - 001_init_users.ts
  - 002_create_users.ts
  - 003_create_refresh_tokens.ts
  - 004_fix_tasks_columns.ts

=== KEY SOURCE FILES (current content) ===

--- src/app.module.ts ---
import { Module } from '@nestjs/common';
import { APP_GUARD } from '@nestjs/core';
import { ThrottlerModule, ThrottlerGuard } from '@nestjs/throttler';
import { DatabaseModule } from './database/database.module';
import { AuthModule } from './auth/auth.module';
import { UsersModule } from './users/users.module';
import { ProjectsModule } from './projects/projects.module';
import { TasksModule } from './tasks/tasks.module';
import { CommentsModule } from './comments/comments.module';
import { AttachmentsModule } from './attachments/attachments.module';
import { NotificationsModule } from './notifications/notifications.module';
import { AuditModule } from './audit/audit.module';
import { DashboardModule } from './dashboard/dashboard.module';
import { OrganizationsModule } from './organizations/organizations.module';
import { JwtAuthGuard } from './auth/auth.guard';
import { RolesGuard } from './auth/roles.guard';

@Module({
  imports: [
    ThrottlerModule.forRoot([
      {
        ttl: 60000,
        limit: 100,
      },
    ]),
    DatabaseModule,
    AuthModule,
    UsersModule,
    ProjectsModule,
    TasksModule,
    CommentsModule,
    AttachmentsModule,
    NotificationsModule,
    AuditModule,
    DashboardModule,
    OrganizationsModule,
  ],
  providers: [
    {
      provide: APP_GUARD,
      useClass: JwtAuthGuard,
    },
    {
      provide: APP_GUARD,
      useClass: RolesGuard,
    },
    {
      provide: APP_GUARD,
      useClass: ThrottlerGuard,
    },
  ],
})
export class AppModule {}


--- src/database/types.ts ---
import { Generated } from 'kysely';

export interface Database {
  organizations: OrganizationsTable;
  users: UsersTable;
  projects: ProjectsTable;
  tasks: TasksTable;
  comments: CommentsTable;
  attachments: AttachmentsTable;
  notifications: NotificationsTable;
  audit_logs: AuditLogsTable;
  refresh_tokens: RefreshTokensTable;
}

export interface OrganizationsTable {
  id: Generated<string>;
  tenant_id: string;
  name: string;
  created_at: Generated<Date>;
}

export interface UsersTable {
  id: Generated<string>;
  tenant_id: string;
  organization_id: string;
  email: string;
  password_hash: string;
  first_name: string;
  last_name: string;
  role: string;
  is_active: Generated<boolean>;
  created_at: Generated<Date>;
  updated_at: Generated<Date>;
}

export interface ProjectsTable {
  id: Generated<string>;
  tenant_id: string;
  organization_id: string;
  name: string;
  description: string | null;
  deadline: string | null;
  status: string;
  created_by: string;
  created_at: Generated<Date>;
  updated_at: Generated<Date>;
}

export interface TasksTable {
  id: Generated<string>;
  tenant_id: string;
  project_id: string;
  assignee_id: string | null;
  title: string;
  description: string | null;
  status: string;
  priority: string | null;
  due_date: string | null;
  created_by: string;
  created_at: Generated<Date>;
  updated_at: Generated<Date>;
}

export interface CommentsTable {
  id: Generated<string>;
  tenant_id: string;
  task_id: string;
  user_id: string;
  content: string;
  created_at: Generated<Date>;
}

export interface AttachmentsTable {
  id: Generated<string>;
  tenant_id: string;
  task_id: string;
  uploaded_by: string;
  file_name: string;
  file_path: string;
  file_size: number;
  created_at: Generated<Date>;
}

export interface NotificationsTable {
  id: Generated<string>;
  tenant_id: string;
  user_id: string;
  type: string;
  message: string;
  read: Generated<boolean>;
  entity_type: string | null;
  entity_id: string | null;
  created_at: Generated<Date>;
}

export interface AuditLogsTable {
  id: Generated<string>;
  tenant_id: string;
  organization_id: string;
  user_id: string | null;
  action: string;
  entity_type: string;
  entity_id: string | null;
  metadata: any | null;
  created_at: Generated<Date>;
}

export interface RefreshTokensTable {
  id: Generated<string>;
  tenant_id: string;
  user_id: string;
  token: string;
  expires_at: Date;
  created_at: Generated<Date>;
}


--- src/database/database.service.ts ---
import { Injectable, OnModuleDestroy } from '@nestjs/common';
import { Kysely, PostgresDialect } from 'kysely';
import { Pool } from 'pg';
import { Database } from './types';

@Injectable()
export class DatabaseService
  extends Kysely<Database>
  implements OnModuleDestroy
{
  constructor() {
    super({
      dialect: new PostgresDialect({
        pool: new Pool({
          connectionString: process.env.DATABASE_URL,
        }),
      }),
    });
  }

  async onModuleDestroy(): Promise<void> {
    await this.destroy();
  }
}


--- src/database/database.module.ts ---
import { Module, Global } from '@nestjs/common';
import { DatabaseService } from './database.service';

@Global()
@Module({
  providers: [DatabaseService],
  exports: [DatabaseService],
})
export class DatabaseModule {}


--- src/auth/auth.service.ts ---
import { Injectable, ConflictException, UnauthorizedException } from '@nestjs/common';
import { JwtService } from '@nestjs/jwt';
import * as bcrypt from 'bcrypt';
import { v4 as uuidv4 } from 'uuid';
import { DatabaseService } from '../database/database.service';
import { RegisterDto } from './dto/register.dto';
import { LoginDto } from './dto/login.dto';
import { JwtPayload } from './jwt.strategy';

@Injectable()
export class AuthService {
  constructor(
    private readonly db: DatabaseService,
    private readonly jwtService: JwtService,
  ) {}

  async register(dto: RegisterDto) {
    const existing = await this.db
      .selectFrom('users')
      .select('id')
      .where('email', '=', dto.email)
      .executeTakeFirst();

    if (existing) {
      throw new ConflictException('Email already registered');
    }

    const password_hash = await bcrypt.hash(dto.password, 12);
    const id = uuidv4();

    const user = await this.db
      .insertInto('users')
      .values({
        id,
        email: dto.email,
        password_hash,
        first_name: dto.first_name || '',
        last_name: dto.last_name || '',
        role: 'Member',
        tenant_id: dto.tenant_id,
        organization_id: dto.organization_id,
      })
      .returningAll()
      .executeTakeFirstOrThrow();

    const payload: JwtPayload = {
      sub: user.id,
      email: user.email,
      role: user.role,
      tenant_id: user.tenant_id,
    };

    return {
      access_token: this.jwtService.sign(payload),
      user,
    };
  }

  async login(dto: LoginDto) {
    const user = await this.db
      .selectFrom('users')
      .selectAll()
      .where('email', '=', dto.email)
      .executeTakeFirst();

    if (!user) {
      throw new UnauthorizedException('Invalid credentials');
    }

    const valid = await bcrypt.compare(dto.password, user.password_hash);
    if (!valid) {
      throw new UnauthorizedException('Invalid credentials');
    }

    const payload: JwtPayload = {
      sub: user.id,
      email: user.email,
      role: user.role,
      tenant_id: user.tenant_id,
    };

    const { password_hash, ...userWithoutPassword } = user;

    return {
      access_token: this.jwtService.sign(payload),
      user: userWithoutPassword,
    };
  }
}


--- src/auth/auth.guard.ts ---
import { Injectable, ExecutionContext } from '@nestjs/common';
import { AuthGuard } from '@nestjs/passport';
import { Reflector } from '@nestjs/core';
import { IS_PUBLIC_KEY } from './public.decorator';

@Injectable()
export class JwtAuthGuard extends AuthGuard('jwt') {
  constructor(private readonly reflector: Reflector) {
    super();
  }

  canActivate(context: ExecutionContext) {
    const isPublic = this.reflector.getAllAndOverride<boolean>(IS_PUBLIC_KEY, [
      context.getHandler(),
      context.getClass(),
    ]);

    if (isPublic) {
      return true;
    }

    return super.canActivate(context);
  }
}


--- src/auth/roles.guard.ts ---
import { Injectable, CanActivate, ExecutionContext, ForbiddenException } from '@nestjs/common';
import { Reflector } from '@nestjs/core';
import { ROLES_KEY } from './roles.decorator';

@Injectable()
export class RolesGuard implements CanActivate {
  constructor(private readonly reflector: Reflector) {}

  canActivate(context: ExecutionContext): boolean {
    const requiredRoles = this.reflector.getAllAndOverride<string[]>(ROLES_KEY, [
      context.getHandler(),
      context.getClass(),
    ]);

    if (!requiredRoles) {
      return true;
    }

    const { user } = context.switchToHttp().getRequest();

    if (!requiredRoles.includes(user.role)) {
      throw new ForbiddenException('Insufficient permissions');
    }

    return true;
  }
}


--- src/auth/jwt.strategy.ts ---
import { Injectable, UnauthorizedException } from '@nestjs/common';
import { PassportStrategy } from '@nestjs/passport';
import { ExtractJwt, Strategy } from 'passport-jwt';
import { DatabaseService } from '../database/database.service';

export interface JwtPayload {
  sub: string;
  email: string;
  role: string;
  tenant_id: string;
}

@Injectable()
export class JwtStrategy extends PassportStrategy(Strategy) {
  constructor(private readonly db: DatabaseService) {
    super({
      jwtFromRequest: ExtractJwt.fromAuthHeaderAsBearerToken(),
      ignoreExpiration: false,
      secretOrKey: process.env.JWT_SECRET || 'changeme-secret-key',
    });
  }

  async validate(payload: JwtPayload) {
    const user = await this.db
      .selectFrom('users')
      .selectAll()
      .where('id', '=', payload.sub)
      .executeTakeFirst();

    if (!user) {
      throw new UnauthorizedException();
    }

    return {
      id: payload.sub,
      email: payload.email,
      role: payload.role,
      tenant_id: payload.tenant_id,
    };
  }
}


--- src/auth/public.decorator.ts ---
import { SetMetadata } from '@nestjs/common';

export const IS_PUBLIC_KEY = 'isPublic';
export const Public = () => SetMetadata(IS_PUBLIC_KEY, true);


--- src/auth/roles.decorator.ts ---
import { SetMetadata } from '@nestjs/common';

export const ROLES_KEY = 'roles';
export const Roles = (...roles: string[]) => SetMetadata(ROLES_KEY, roles);


--- src/auth/dto/register.dto.ts ---
import { IsEmail, IsString, MinLength, IsUUID, IsOptional } from 'class-validator';

export class RegisterDto {
  @IsEmail()
  email: string;

  @IsString()
  @MinLength(8)
  password: string;

  @IsString()
  tenant_id: string;

  @IsUUID()
  organization_id: string;

  @IsOptional()
  @IsString()
  first_name?: string;

  @IsOptional()
  @IsString()
  last_name?: string;
}


--- src/auth/dto/login.dto.ts ---
import { IsEmail, IsString } from 'class-validator';

export class LoginDto {
  @IsEmail()
  email: string;

  @IsString()
  password: string;
}



=== YOUR TASK ===
DIRECT AGENT INSTRUCTION:

You are Claude Code working inside a real git repository.
Implement exactly ONE backend story.
Modify and create repository files directly.
Do not explain. Do not summarize. Do not output markdown. Only make code changes.

Stack: NestJS + Kysely + PostgreSQL

BEFORE WRITING CODE — read and understand these files:
- src/app.module.ts
- src/database/database.service.ts
- src/database/types.ts
- src/auth/auth.guard.ts
- src/auth/jwt.strategy.ts
- src/auth/public.decorator.ts
- src/auth/roles.decorator.ts

BEFORE implementing the story, fix these bugs in existing files:
1. Replace req.user.sub with req.user.id in all controllers
2. Remove all @UseGuards() from controllers — guards are global via APP_GUARD
3. Change @Roles('ADMIN') to @Roles('Admin') everywhere
4. Remove role field from src/auth/dto/register.dto.ts
5. Change auth.service.ts to use hardcoded 'Member' instead of dto.role
6. Delete src/modules/database/ folder entirely
7. Delete src/database/kysely.module.ts
8. Delete src/database/database.types.ts
9. Remove KyselyModule import from app.module.ts
10. Add RefreshTokensTable and ProjectMembersTable to src/database/types.ts
12. In src/database/types.ts, make sure EVERY table interface has tenant_id: string as a property — if it is missing, add it
13. In ALL service files, replace .select(['col1', 'col2', ...]) with .selectAll() — Kysely does not accept string arrays in .select()
14. In src/database/types.ts fix Generated<> usage: only id, created_at, updated_at, is_active should use Generated<>. All other columns (tenant_id, email, name, role, password_hash, organization_id, created_by, project_id, etc.) must be plain string or string | null
15. In tasks service and DTO files, replace any occurrence of 'TODO' with 'ToDo', 'IN_PROGRESS' with 'InProgress', 'DONE' with 'Completed'
16. Make sure TasksTable has assignee_id: string | null as a property
THEN implement the story task below.

Task:
DIRECT AGENT INSTRUCTION:

You are Claude Code operating inside a real git repository.
Stack: NestJS 10 + Kysely 0.27 + PostgreSQL
You have full access to read, create, modify files and run shell commands.
Do not explain. Do not produce markdown. Only make file changes and run commands.

PHASE 1 — READ FIRST (mandatory, do not skip)
Read these files completely before writing any code:
1. ARCHITECTURE.md — your law. Follow every pattern exactly.
2. src/app.module.ts
3. src/database/types.ts
4. src/database/database.service.ts
5. src/auth/auth.guard.ts
6. src/auth/jwt.strategy.ts
7. src/auth/public.decorator.ts
8. src/auth/roles.decorator.ts
If any file does not exist yet, skip it and continue.

PHASE 2 — APPLY BUG FIXES (scan all existing files, fix silently)
FIX 1: req.user.sub to req.user.id in ALL controllers
FIX 2: Remove ALL @UseGuards() from ALL controllers
FIX 3: @Roles('ADMIN') to @Roles('Admin') everywhere
FIX 4: Remove role field from src/auth/dto/register.dto.ts if present
FIX 5: In auth.service.ts hardcode role: 'Member' instead of dto.role
FIX 6: Delete src/modules/database/ if it exists
FIX 7: Delete src/database/kysely.module.ts if it exists
FIX 8: Delete src/database/database.types.ts if it exists
FIX 9: Remove KyselyModule from app.module.ts imports if present
FIX 10: In ALL services replace .select(['col1','col2']) with .selectAll()
FIX 11: In types.ts make tenant_id plain string on EVERY table interface
FIX 12: In types.ts Generated<> only on id, created_at, updated_at, is_active, read
FIX 13: In tasks files replace 'TODO' with 'ToDo', 'IN_PROGRESS' with 'InProgress', 'DONE' with 'Completed'
FIX 14: Make sure TasksTable has assignee_id: string | null

PHASE 3 — ARCHITECTURE RULES (follow exactly)

GLOBAL GUARDS:
Guards are registered globally via APP_GUARD in app.module.ts.
NEVER add @UseGuards() to any controller or method — not even once.

AUTHENTICATION:
JWT Payload into token: { sub: userId, email, role, tenant_id }
req.user after validation: { id, email, role, tenant_id }
ALWAYS use req.user.id — NEVER use req.user.sub
jwt.strategy.ts validate() must return exactly:
  return { id: payload.sub, email: payload.email, role: payload.role, tenant_id: payload.tenant_id }

ROLE VALUES — exact casing:
'Admin' | 'ProjectManager' | 'Member'
WRONG: 'ADMIN', 'admin', 'MEMBER'

STATUS VALUES — exact casing:
Task: 'ToDo' | 'InProgress' | 'Completed'
Project: 'Active' | 'Archived'
WRONG: 'TODO', 'IN_PROGRESS', 'DONE'

REGISTRATION RULE:
Register endpoint NEVER accepts role from body. Hardcode: role: 'Member'

KYSELY SELECT RULES:
NEVER: .select(['id', 'email']) — TypeScript error, Kysely rejects arrays
CORRECT: .selectAll()
CORRECT: .select('id').select('email')

KYSELY Generated<T> RULES:
Generated<> ONLY for: id, created_at, updated_at, is_active, read
Everything else is plain string or string | null:
  tenant_id        → string (never Generated)
  email            → string
  role             → string
  organization_id  → string
  created_by       → string
  assignee_id      → string | null
  description      → string | null
  due_date         → string | null
  deadline         → string | null

COMPLETE types.ts — use this exact content:
import { Generated } from 'kysely';
export interface Database {
  organizations: OrganizationsTable;
  users: UsersTable;
  projects: ProjectsTable;
  tasks: TasksTable;
  comments: CommentsTable;
  attachments: AttachmentsTable;
  notifications: NotificationsTable;
  audit_logs: AuditLogsTable;
  refresh_tokens: RefreshTokensTable;
}
export interface OrganizationsTable {
  id: Generated<string>;
  tenant_id: string;
  name: string;
  created_at: Generated<Date>;
}
export interface UsersTable {
  id: Generated<string>;
  tenant_id: string;
  organization_id: string;
  email: string;
  password_hash: string;
  first_name: string;
  last_name: string;
  role: string;
  is_active: Generated<boolean>;
  created_at: Generated<Date>;
  updated_at: Generated<Date>;
}
export interface ProjectsTable {
  id: Generated<string>;
  tenant_id: string;
  organization_id: string;
  name: string;
  description: string | null;
  deadline: string | null;
  status: string;
  created_by: string;
  created_at: Generated<Date>;
  updated_at: Generated<Date>;
}
export interface TasksTable {
  id: Generated<string>;
  tenant_id: string;
  project_id: string;
  assignee_id: string | null;
  title: string;
  description: string | null;
  status: string;
  priority: string | null;
  due_date: string | null;
  created_by: string;
  created_at: Generated<Date>;
  updated_at: Generated<Date>;
}
export interface CommentsTable {
  id: Generated<string>;
  tenant_id: string;
  task_id: string;
  user_id: string;
  content: string;
  created_at: Generated<Date>;
}
export interface AttachmentsTable {
  id: Generated<string>;
  tenant_id: string;
  task_id: string;
  uploaded_by: string;
  file_name: string;
  file_path: string;
  file_size: number;
  created_at: Generated<Date>;
}
export interface NotificationsTable {
  id: Generated<string>;
  tenant_id: string;
  user_id: string;
  type: string;
  message: string;
  read: Generated<boolean>;
  entity_type: string | null;
  entity_id: string | null;
  created_at: Generated<Date>;
}
export interface AuditLogsTable {
  id: Generated<string>;
  tenant_id: string;
  organization_id: string;
  user_id: string | null;
  action: string;
  entity_type: string;
  entity_id: string | null;
  metadata: any | null;
  created_at: Generated<Date>;
}
export interface RefreshTokensTable {
  id: Generated<string>;
  user_id: string;
  token: string;
  expires_at: Date;
  created_at: Generated<Date>;
}

DATABASE PATTERNS:
(Use exactly as defined above.)

CONTROLLER PATTERN — NO @UseGuards() anywhere:
(Use exactly as defined above.)

MODULE PATTERN:
(Use exactly as defined above.)

FILES THAT MUST NEVER BE DUPLICATED:
Only ONE DatabaseModule: src/database/database.module.ts
Only ONE types.ts: src/database/types.ts
Only ONE app.module.ts: src/app.module.ts
NEVER create: src/modules/database/, src/database/kysely.module.ts, src/database/database.types.ts

ADDING A NEW MODULE CHECKLIST:
(Apply if needed.)

ABSOLUTE RULES — VIOLATION MEANS FAILURE:
(Apply all 13 rules exactly.)

PHASE 4 — IMPLEMENT THE STORY

STORY TO IMPLEMENT:
Story Number: 1
Title: Database Foundation

Description:
Establish the global database module, full schema types, and a single migration that creates all tables with constraints and indexes.

Files to create:
- src/database/database.module.ts
- src/database/database.service.ts
- src/database/types.ts
- src/database/migrations/001_initial.ts
- src/database/migrate.ts

Files to modify:
- src/app.module.ts

Acceptance Criteria:
- DatabaseModule is decorated with @Global() and exports DatabaseService
- DatabaseService extends Kysely<Database> and connects using process.env.DATABASE_URL
- types.ts defines Database interface including tables: organizations, users, projects, tasks, comments, notifications, audit_logs — every table except organizations has tenant_id: string (NOT Generated<string>)
- Generated<> is used ONLY for: id, created_at, updated_at, is_active, read columns
- 001_initial.ts creates ALL tables (organizations, users, projects, tasks, comments, notifications, audit_logs) with correct columns, CHECK constraints for roles and statuses, foreign keys with ON DELETE CASCADE, and all indexes defined in requirements
- users.role CHECK constraint enforces exactly: 'Admin','ProjectManager','Member'
- projects.status CHECK constraint enforces exactly: 'Active','Archived'
- tasks.status CHECK constraint enforces exactly: 'ToDo','InProgress','Completed'
- All tables include created_at DEFAULT now() and updated_at DEFAULT now()
- Indexes created: idx_users_tenant, idx_users_email, idx_projects_tenant, idx_tasks_tenant, idx_tasks_project, idx_tasks_assignee, idx_comments_tenant, idx_comments_task
- migrate.ts executes all pending migrations on application bootstrap
- app.module.ts imports DatabaseModule

Implement everything fully before proceeding.

PHASE 5 — VERIFY BUILD
Run: npx tsc --noEmit
Fix all errors. Never use @ts-ignore. Repeat until zero errors.
Do NOT proceed to Phase 6 with a broken build.

PHASE 6 — COMMIT AND PUSH
Run in exact order:
  git add -A
  git commit -m "feat: story-1 Database Foundation"
  git push origin HEAD

After push succeeds make this HTTP POST request to trigger the next story:
  POST http://localhost:5678/webhook-test/ai%20automation
  Content-Type: application/json
  Body:
  {
    "project": "ai-automation",
    "webhookUrl": "http://localhost:5678/webhook-test/ai%20automation",
    "state": {
      "completed_stories": [1],
      "current_story": null
    }
  }

After POST succeeds output exactly:
=== STORY 1 COMPLETE — NEXT STORY TRIGGERED ===